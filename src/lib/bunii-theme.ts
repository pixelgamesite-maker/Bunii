// Shared design tokens for the Bunii site. Keep every component pulling
// from here so a future palette/font change happens in one place.

export const FONT_LINK =
  "https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;0,700;1,400;1,600&display=swap";

export const serif = "'Cormorant Garamond', Georgia, serif";
export const sans = "'Segoe UI', 'Helvetica Neue', Arial, sans-serif";

// NOTE: kept the accent hex from the original template. Swap these two
// if Bunii ends up with its own brand color.
export const gold = "#6fa8c4";
export const goldLight = "#8ec5df";

export const bg = "#050504";
